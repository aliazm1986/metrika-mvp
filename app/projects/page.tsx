import Link from "next/link";
import Image from "next/image";
import { Search, SlidersHorizontal, ArrowLeft } from "lucide-react";
import { projects } from "@/lib/data";
export default function ProjectsPage(){return <main><nav className="nav"><Link href="/" className="brand"><span className="brand-mark">M</span><span>مِتریکا</span></Link><div className="nav-links"><Link href="/projects">پروژه‌ها</Link><Link href="/marketplace">بازار ثانویه</Link><Link href="/how-it-works">چطور کار می‌کند؟</Link></div><Link href="/dashboard" className="primary-btn">داشبورد</Link></nav><div className="container page-head"><span className="eyebrow">کتابخانه پروژه‌ها • داده نمایشی</span><h1>پروژه مناسب خود را پیدا کنید.</h1><p>فیلتر کنید، مقایسه کنید و قبل از هر تصمیم، اسناد و ریسک‌ها را بخوانید.</p></div><div className="container filters"><div className="search"><Search size={18}/><input placeholder="جست‌وجوی نام، شهر یا کد پروژه"/></div><button className="filter-btn"><SlidersHorizontal size={17}/> فیلترهای بیشتر</button><div className="chips"><span className="chip active">همه</span><span className="chip">مسکونی</span><span className="chip">تجاری</span><span className="chip">ویلایی</span><span className="chip">ریسک پایین</span></div></div><div className="container project-grid listing">{projects.map((p)=><Link href={`/projects/${p.slug}`} className="project-card" key={p.slug}><div className="project-art"><Image src={p.image} alt={`رندر نمایشی ${p.name}`} fill sizes="(max-width: 800px) 100vw, 33vw" style={{objectFit:"cover"}}/><span>{p.stage}</span><div className="art-label">{p.code}</div></div><div className="project-body"><div className="project-top"><div><h3>{p.name}</h3><p>{p.city} • {p.type}</p></div><span className={`risk ${p.risk}`}>{p.riskLabel}</span></div><div className="metric-row"><div><small>حداقل ورود</small><b>{new Intl.NumberFormat("fa-IR").format(p.min)} <i>تومان</i></b></div><div><small>تأمین‌شده</small><b>{p.funding}%</b></div></div><div className="progress"><span style={{width:`${p.funding}%`}}/></div><div className="card-link">مشاهده پروژه <ArrowLeft size={15}/></div></div></Link>)}</div></main>}
